import { ProfilePreferences } from '@/shared/ui/profile-preferences/profile-preferences';

export const CompleteRegistrationPage = () => {
  return (
    <div className="flex items-center justify-center h-screen px-3">
      <ProfilePreferences />
    </div>
  );
};
